# Archived Planning Documents

Files in this directory are historical context only. Future work must use
[`docs/implementation-backlog.md`](../implementation-backlog.md), which is the single active
backend/frontend development truth and uses the `AUD-*` and `VS-*` vertical-slice queue.

The archived frontend ledger is retained so future models can understand past commits without
mistaking its old `FE-*` layer-oriented plan or deferred browser-testing rules for current policy.
